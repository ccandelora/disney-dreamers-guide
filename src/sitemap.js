/*import axios from "axios";*/
/*
const sitemap = require('sitemap');
    const url ="https://oyster-app-sus4c.ondigitalocean.app/posts";
    const postData = async (url, data) => {
        const response = await axios.post(url, data);
        console.log(response.data);
        return response.data;
    };
    const hostname = 'https://www.disneydreamersguide.com';

    const homeUrls = [
      { url: '/', changefreq: 'daily', priority: 1 },
      { url: '/articles', changefreq: 'monthly', priority: 0.8 },
      { url: '/epcot-queue-times', changefreq: 'monthly', priority: 0.8 },
        { url: '/magic-kingdom-queue-times', changefreq: 'monthly', priority: 0.8 },
        { url: '/hollywood-studios-queue-times', changefreq: 'monthly', priority: 0.8 },
        { url: '/animal-kingdom-queue-times', changefreq: 'monthly', priority: 0.8 },
      // Add additional pages here
    ];

    const sitemapInstance = sitemap.createSitemap({
      hostname,
      homeUrls,
    });
    */